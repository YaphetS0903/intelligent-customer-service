# 智能客服目标验收矩阵

更新时间：2026-07-12

## 已验证闭环

| 目标项 | 当前状态 | 证据 |
| --- | --- | --- |
| 课件视频后台任务 | 已完成并生产验证 | `/api/training/[id]/slide-video` 立即返回 202；后台 `training_video_jobs` 写入 queued/generating/ready |
| 视频生成进度 | 已完成并生产验证 | 管理端展示课件画面、语音、视频进度；真实 90 页 PPT 任务显示 90/90 |
| 课程语音后台任务 | 已完成并生产验证 | 新增 `/api/training/[id]/audio-job`，立即返回 202；后台以 `provider=training-audio` 写入 queued/generating/ready，逐页补齐 TTS 缓存并展示进度/失败重试 |
| 失败重试/重新生成 | 已完成 | 已失败或已完成视频可再次点击生成新任务；运行中任务会复用当前任务 |
| 课程删除/归档/发布 | 已完成 | 草稿/归档可删除；已发布课程需先归档；员工端只显示 published + ready |
| 正式考试与完课规则 | 已完成并生产验证 | 支持单选、多选、判断题，题库初稿/编辑/发布、及格分、次数和时限；学习进度 100% 后才可开考，启用考试时通过考试才计为完课 |
| 培训证书 | 已完成并生产验证 | 考试通过后自动签发唯一编号证书，支持中文 PDF 下载和管理员撤销；撤销后下载返回 410 |
| 必修课程与提醒 | 已完成并生产验证 | 支持必修标记、截止日期、发布通知、未完课/逾期提醒和每日去重 |
| 真实 PPT 知识库入库 | 已完成并生产验证 | `开始云-Kass产品培训资料.pptx` 入库成功，90 页、90 分片、已发布 |
| 真实 PPT 对话检索 | 已完成并生产验证 | 生产 `/api/chat` 返回 200，答案带 6 个引用来源 |
| 真实 PPT 课件视频 | 已完成并生产验证 | 90 页课件画面、90 页语音、MP4 返回 `206 video/mp4`，大小约 26.8MB |
| 引用来源精简 | 已完成 | 员工端默认显示“已引用 N 个来源”，点击抽屉查看详情 |
| 员工端/管理员端角色分流 | 已完成并生产验证 | 员工访问首页会直接进入 `/chat`，不再读取或展示全局运营数据和管理入口；管理员保留运营工作台，侧栏按“员工端预览/管理后台”分组并高亮当前位置；手机端改为可展开完整导航菜单，不再只显示前 6 项 |
| 最近会话归档/删除 | 已完成 | 最近会话先归档，归档区可永久删除 |
| 无引用/无答案任务 | 已完成 | `lib/no-citation-task.ts` 自动生成待补充知识任务 |
| 资料上传失败重试 | 已完成 | 批量上传会逐文件返回结果；失败文件保留原 `File`、知识库、部门和版本说明，可在失败面板单独重试、查看失败原因或移除记录 |
| QA 测试集与批量运行 | 已完成 | `/admin/qa-tests` 支持创建、导入、批量运行、失败标记、成本统计 |
| QA 失败一键补知识 | 已完成并生产验证 | 失败、无引用、低覆盖和未命中题可直接填写补充内容生成知识资料，并立即复测；也可批量生成 QA 整改任务进入整改闭环 |
| 模型回答质量评分 | 已完成并生产验证 | QA 后台按引用、期望答案关键词覆盖、知识库命中、响应耗时和人工评审计算质量分；顶部展示质量均分，单题展示分数、等级、扣分项和建议动作 |
| 多模型切换与自动降级 | 已完成基础闭环 | 配置页支持主模型及备用 1/备用 2 Provider、Base URL、API Key、模型 ID；非流式和流式调用会按主模型、备用 1、备用 2 顺序尝试，并在用量元数据记录 `model_attempts` |
| 检索评估看板 | 已完成基础版 | QA 后台聚合风险率、平均引用、覆盖均值，并按知识库、命中文档/位置、高风险问题归因 |
| QA 失败反查与意图分类 | 已完成并生产验证 | QA 后台按登录账号、资料上传、培训、安全质量、审批、反馈等意图归类风险题；失败问题可回溯命中来源并深链到资料分片治理 |
| 召回贡献度诊断 | 已完成并生产验证 | 本地 RAG 引用会记录正文、标题、文件名、段落、摘要、关键词、同义词、语义、结构等 `match_signals`；QA 后台展示“召回贡献”汇总和单条来源标签，老记录无分项时提示复跑后生成 |
| 召回策略 A/B 对比 | 已完成并生产验证 | QA 后台支持只读对比当前平衡、正文优先、摘要关键词增强、同义词扩展策略；生产 42 条 QA、16 个文档、110 个分片返回 4 组策略指标，不调用模型、不写入数据库 |
| 召回策略灰度开关 | 已完成并生产验证 | 配置页新增 `RAG_RETRIEVAL_STRATEGY`，支持 `balanced/content_first/governance_enhanced/synonym_expanded`；非流式和流式本地 RAG 对话都会读取该配置，模型用量元数据记录实际策略 |
| 治理前后效果对比 | 已完成并生产验证 | QA 后台支持只读模拟当前治理字段或待确认建议应用前后的覆盖率、误召回风险、命中来源变化；生产 42 条 QA、16 个文档、110 个分片返回 200，页面 390px/1440px 无横向溢出 |
| 自动治理动作建议 | 已完成并生产验证 | QA 后台新增只读自动建议，综合 QA 样本、召回覆盖、分片治理字段、待确认建议和策略对比，输出补知识、治理分片、调策略、复核误召回四类动作；生产 42 条 QA、16 个文档、110 个分片生成 18 条建议、7 条高优先级；建议面板可一键为高优先级建议生成 QA 整改任务，并汇总唯一待治理分片队列、打开首个分片、复制治理链接清单 |
| QA 整改复测闭环 | 已完成并生产验证 | QA 列表接口返回 `remediationByTestId` 和整改读取状态；QA 后台新增“整改闭环”面板，展示整改任务、待处理、处理中、复测通过、已复测和最近复测；单题卡片展示已建整改、原因、建议、缺失关键词和最近复测摘要；整改任务读取慢时有明确超时提示并可重读 |
| QA 整改自动复测队列 | 已完成并生产验证 | 新增 `/api/admin/knowledge-tasks/retest-batch-job` 后台队列，支持待处理/处理中 QA 整改任务批量复测、轮询进度、停止队列和完成 Toast；QA 后台“整改任务与自动复测”面板新增“复测待整改”和队列进度条 |
| QA 复测趋势报表 | 已完成并生产验证 | QA 接口聚合整改任务 note 中的全部复测记录，返回累计复测、覆盖任务数、通过/仍需整改/异常忽略、最近 7 天趋势和最近复测；QA 后台在整改闭环面板内展示趋势小面板 |
| QA 定时自动复测计划 | 已完成并生产验证 | 新增 `/api/admin/knowledge-tasks/retest-schedule`，支持读取、保存开启状态/间隔/单次上限，并按计划立即启动复测；计划配置持久化到 `.data/knowledge-task-retest-schedule.json`，执行仍复用后台复测队列；QA 后台新增紧凑计划控制区 |
| 知识治理后自动排队复测 | 已完成基础版并生产部署 | 分片手动治理、拆分、合并、批量保存 AI 治理建议后，会按受影响知识库查找待处理/处理中的 QA 整改任务，并用现有后台复测队列精准排队；接口返回 `retest_queue`，知识库后台成功提示会显示已排队数量或跳过原因 |
| RAG 策略切换后自动排队复测 | 已完成基础版并生产部署 | 配置页保存时若 `RAG_PROVIDER` 进入本地文本模式或 `RAG_RETRIEVAL_STRATEGY` 发生变化，会自动查找待处理/处理中的 QA 整改任务并创建复测队列；保存配置 Toast 会提示已排队数量，用于观察策略变更效果 |
| RAG 策略效果长期趋势 | 已完成并生产验证 | QA 运行记录写入 `retrieval_strategy`、引用数、期望覆盖和耗时；问答测试后台展示“策略效果趋势”，按策略和日期汇总通过率、无引用率、覆盖率、耗时和 token，旧数据标记为“历史未记录”；支持异常提醒、按知识库拆分、按问题类型拆分、时间范围筛选和策略切换前后对比 |
| RAG 策略效果按部门/岗位拆分 | 已完成并生产验证 | QA 策略趋势会按运行人或测试创建人的部门、岗位归因，输出通过率、无引用率、覆盖率、耗时、token 和风险分；异常提醒会补充高风险部门/岗位，帮助定位哪些人群需要补资料或调整可见范围 |
| RAG 策略异常一键转整改 | 已完成并生产验证 | 策略异常会关联低覆盖、无引用或不通过的 QA 题，异常卡片可直接“生成相关整改”；后端复用现有 QA 整改任务流，跳过已有任务，生成后刷新整改闭环 |
| RAG 策略异常定时巡检 | 已完成并生产验证 | QA 后台新增“策略异常巡检”，支持读取/保存计划、开启/暂停、设置巡检间隔/样本窗口/单次上限，并可立即扫描最近 QA 样本自动生成整改；已存在的整改任务会跳过，计划状态持久化到 `.data/qa-strategy-anomaly-schedule.json` |
| RAG 策略异常运营告警入口 | 已完成并生产验证 | `/api/admin/insights` 聚合 QA 策略异常巡检结果并返回 `operationAlerts`；洞察页顶部展示紧凑“运营告警”横幅，显示候选/新增/跳过/待处理 QA 整改指标，支持一键进入 QA 整改或巡检计划；`/admin/insights?tab=qa` 可深链打开 QA 整改 |
| OCR 配置与测试 | 已完成基础闭环 | 配置页支持 OCR 表单与测试，知识库处理支持 OCR 阶段、失败原因、重新识别 |
| OCR/PDF 原图分页预览 | 已完成并生产验证 | 资料预览支持原页图像 + 识别文本/分片治理对照；普通桌面和手机先上下排列，超宽屏再并排；预览接口偶发失败会自动重试，最终失败显示“重试预览” |
| 资料版本与回滚 | 已完成 | 资料版本记录、正文快照、比较、回滚接口和界面已具备 |
| 资料回滚体验优化 | 已完成并生产验证 | 版本对比面板新增未变化/已变更/新增/删除概览，分片以历史版本/当前内容左右对照展示；新增/删除/变更内容使用状态底色和文本高亮，回滚前可更直观看差异 |
| 知识库批量操作 | 已完成基础版 | 管理端支持批量提交审核、发布、归档、重新识别 |
| 知识分片质量面板 | 已完成基础版 | `/api/documents` 返回空分片、过短、超长、疑似 OCR 噪声、质量分；知识库后台展示质量面板并可直接预览/重新识别 |
| 知识分片治理进阶 | 已完成并生产验证 | 预览面板支持分片摘要、关键词、同义词、手动拆分、相邻合并；拆分/合并自动生成治理前版本快照；本地 RAG 已纳入摘要/同义词参与召回 |
| 批量分片摘要建议 | 已完成基础版并生产验证 | 资料预览页支持对当前页面/段落最多 12 个缺失治理信息的分片生成 AI 摘要、关键词、同义词建议；管理员确认填入后再保存入库 |
| 全库分片治理队列 | 已完成基础版并生产验证 | 知识库后台支持跨当前知识库扫描缺摘要/关键词/同义词分片，后台分批生成 `pending_suggestion` 待确认建议；建议不会自动覆盖正式知识，管理员需在预览中填入表单并保存 |
| 分片治理建议确认台 | 已完成并生产验证 | 知识库后台集中展示待确认治理建议，支持当前知识库/全部知识库切换、多选、批量保存、批量撤销、打开定位到原分片；批量保存会写入正式摘要/关键词/同义词并清理 `pending_suggestion` |
| 知识治理审计 | 已完成并生产验证 | 分片治理建议保存/撤销、手动治理、拆分、合并会写入分片 `metadata.governance_audit`；知识库后台展示最近治理审计，可查看动作、资料/分片、操作人、前后变化并定位原分片 |
| 成本统计 | 已完成基础版 | 对话、QA、培训 TTS/视频写入 model usage，洞察页展示 token/成本 |
| 热门问题统计 | 已完成 | 洞察页展示热门问题和会话指标 |
| 全局 Toast/错误重试 | 已完成基础版并生产验证 | 员工聊天使用统一 Toast、失败回答有重试按钮；培训列表、培训后台、QA 后台、知识库后台、部署检查、试运行验收初始加载失败显示页内重试框，模拟 500 后可恢复；资料预览接口偶发失败会自动重试，最终失败可点“重试预览”；知识库后台高频操作、部署检查测试/导出、试运行导出/生成测试模板已接入 Toast |
| 后台低频操作确认 | 已完成基础版并生产部署 | 知识库后台删除知识库/资料、批量提交审核/发布/归档/重识别/删除、全库治理队列、治理建议保存/撤销、资料回滚，以及培训课程删除均改为统一页内确认弹窗；线上源码已无 `window.confirm` |
| 站内通知中心 | 已完成基础闭环并生产验证 | 新增 `/notifications`、通知数据表和已读 API；审批待办/驳回/发布、工单创建/分派/回复/结果、高危安全事件和 QA 异常巡检均接入通知；支持全部/未读/分类筛选、单条已读/未读、全部已读和业务跳转；通知使用业务事件去重键，并预留通用、邮件、企业微信 Webhook；本地 `typecheck/build` 通过，工单通知与审批通知生产回归 2/2 通过，390px 手机无横向溢出，测试账号、资料、工单和关联通知已精确清理。 |
| 自动化测试/CI 回归底座 | 已完成本地验证 | 新增 `.github/workflows/ci.yml` 和 `.github/workflows/deploy.yml`，CI/部署校验均使用 MySQL 8 临时服务、`npm ci`、Playwright Chromium、typecheck、build 和 e2e；Playwright 自动整理演示数据，并新增 QA 整改复测队列与复测趋势断言 |

## 已实现但建议继续打磨

| 目标项 | 当前状态 | 后续建议 |
| --- | --- | --- |
| 移动端适配 | 培训后台、QA 后台、知识库后台、配置页、用户管理、洞察页已验证 | 培训任务、学习跟踪、QA 测试卡片、知识库资料卡片、预览面板、配置表单、用户编辑、洞察会话/用量明细已按手机宽度验收 |
| 全局反馈统一 | 培训后台、QA 后台、知识库后台、配置页、用户管理、洞察页、部署检查、试运行验收已接入 Toast；知识库/培训低频危险操作已接入统一确认弹窗 | 后续继续检查更低频的报表、下载和批处理操作是否仍有局部提示 |
| 加载 skeleton/空状态 | 培训列表、培训后台、QA 后台、知识库后台、配置页、用户管理、洞察页、部署检查、试运行验收已完成并生产验证 | 后续继续补更低频管理面板和更细空状态 |
| 员工端/管理员端视觉区分 | 已完成主要闭环并生产验证 | 员工端使用轻量白底/绿色身份栏并直接进入问答；管理端使用深色身份栏、管理当前位置条、分组导航和独立状态色；后续仅需随新增页面保持这一规范 |
| 检索命中率 | 已做混合检索、分片摘要/同义词治理、QA 评估看板、失败反查、召回贡献度、召回策略 A/B 对比、召回策略灰度开关、治理前后效果对比、自动治理动作建议、QA 整改复测闭环、QA 整改自动复测队列、QA 定时自动复测计划、知识治理后自动排队复测、RAG 策略切换后自动排队复测、RAG 策略效果长期趋势、按部门/岗位拆分、策略异常一键转整改、策略异常定时巡检、站内运营告警入口、当前页批量建议、全库治理队列和确认台批量落库 | 后续可加企业微信/邮件外部推送，并按具体用户、角色、密级继续细分 |
| 知识分片质量 | 已有基础面板、手动治理、QA 分片反查、自动治理动作建议、当前页批量建议、全库治理队列、确认台批量落库、治理审计和治理效果对比 | 后续可加资料差异高亮和分片质量趋势 |

## 仍未完成或需外部条件

| 目标项 | 当前状态 | 阻塞/说明 |
| --- | --- | --- |
| 真正第三方数字人视频 | 未完成 | 当前课件视频已可生产使用；数字人仍需要接具体第三方数字人 API |
| SSO/LDAP 正式联调 | 基础配置已做 | 需要企业身份源参数和回调域名/HTTPS |
| 更细权限体系 | 部分完成 | 已有角色、部门、岗位、资料密级、ACL；审批流更细节点可继续扩展 |
| GitHub 云端 CI/CD 首次运行 | 待外部条件 | 本地已沉淀 CI/部署 workflow；还需要把代码推到 GitHub 仓库，并配置 `DEPLOY_HOST`、`DEPLOY_USER`、`DEPLOY_SSH_KEY`、`DEPLOY_PORT`、`DEPLOY_APP_DIR` 等 GitHub Secrets 后触发首次云端验证/部署 |
| 敏感词/提示词注入面板/异常告警 | 已完成基础版并生产验证 | 已有敏感输入/输出脱敏、提示词注入、异常访问、连续风险触发记录；洞察页安全审计支持风险概览、类型/等级/状态筛选、事件片段查看、会话定位和状态处置；高危/严重事件已接入站内通知，后续可继续增加规则配置 |

## 最近生产验证记录

| 验证项 | 结果 |
| --- | --- |
| 生产地址 | 已部署（地址不写入公开仓库） |
| 健康检查 | 红项 0，黄项 4 |
| 部署检查 | 分数 93，红项 0，黄项 4 |
| 真实 PPT 文档 | `开始云-Kass产品培训资料.pptx` |
| 知识库结果 | 90 页、90 分片、状态 ready/published |
| 对话结果 | `/api/chat` 200，引用 6 个来源 |
| 培训结果 | 90 页讲稿、90 页课件图、90 页语音 |
| 视频结果 | ready，`206 video/mp4`，约 26.8MB |
| 课程语音后台预生成 | 本地 `npm run build && npm run typecheck` 通过；远端 `npm run build && npm run typecheck` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产新增 `/api/training/[id]/audio-job` 路由；演示课程真实后台任务 `video-2ec7df65-07fd-418d-bdfc-e43e5caa4d94` 从 queued/generating 到 ready，3/3 页语音、进度 100%；`/admin/training` 1440px/390px 无横向溢出、无控制台错误，展示“语音已缓存/语音未预生成”和“课程语音已缓存/预生成课程语音”按钮状态 |
| 课程视频播放兜底 | 课程视频读取接口在远程数据库慢时可使用培训列表快照兜底；生产真实 Kass 视频 Range 复验返回 `206 Partial Content`、`content-type: video/mp4`、`content-range: bytes 0-1023/26757183` |
| 培训后台 UI | 390px 手机无横向溢出、显示课程卡片；1440px 桌面显示 2 张表格；控制台无报错 |
| QA 后台 UI | 390px 手机无横向溢出、42 张测试卡片可见、按钮触控高度正常；1440px 桌面无控制台报错 |
| 模型质量评分/多模型降级回归 | 本地 `npm run typecheck && npm run build` 通过；本地配置页与 QA 后台 Playwright 2 条通过；生产 QA 回归展示“质量均分”和单题“质量评分”，配置页展开“自定义对话模型”后展示备用 1/备用 2 Provider 与模型 ID；生产静态 JS 偶发 `ERR_EMPTY_RESPONSE` 时测试会以客户端就绪标志自动刷新重试，最终两项均通过 |
| QA 检索评估看板 | 生产 390px/1440px 无横向溢出，展示检索评估看板、知识库风险归因、命中文档/位置，42 张测试卡片正常 |
| QA 失败反查与分片定位 | 生产 `/admin/qa-tests` 390px/1440px 无横向溢出、无控制台错误，展示“QA 失败反查”；生产 `/admin/documents?document=...&chunk=...` 可自动定位并高亮“QA 定位”分片 |
| QA 召回贡献度 | 生产 `/admin/qa-tests` 展示“召回贡献”面板；390px/1440px 无横向溢出；服务器只读召回验证返回 `firstDominant=content`，`match_signals` 包含 `content/title/file_name/semantic/proximity/structural/recency` |
| QA 召回策略 A/B 对比 | 生产 `/api/admin/qa-tests/retrieval-strategies?limit=60` 返回 42 条样本、16 个文档、110 个分片，最优策略 `content_first`；页面点击“运行对比”后展示四种策略卡片和策略差异问题；390px/1440px 无横向溢出 |
| RAG 召回策略灰度开关 | 本地和远端 `npm run typecheck && npm run build` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产 `/admin/settings` 点击“RAG 检索模式”后显示 `RAG_RETRIEVAL_STRATEGY=balanced`，支持四种策略占位提示；390px/1440px 无横向溢出、无控制台错误 |
| RAG 策略切换后自动排队复测 | 本地 `npm run build && npm run typecheck` 通过；远端 `npm run build && npm run typecheck` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产只读冒烟验证 `/api/system/settings` 返回 200，当前 `RAG_PROVIDER=local_text`、`RAG_RETRIEVAL_STRATEGY=balanced`；`/admin/settings` 390px/1440px 无横向溢出、无控制台错误；未修改线上策略，避免触发真实复测写入 |
| RAG 策略效果长期趋势 | 本地 `npm run build && npm run typecheck` 通过；本地 Playwright 只读用例通过；远端 `npm run build` 和 `npm run typecheck` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产 Playwright 只读用例验证 `/admin/qa-tests` 展示“策略效果趋势”“策略切换前后对比”“按知识库拆分”“按问题类型拆分”，`/api/admin/qa-tests` 返回 `strategyTrend.window/comparison/event_count/current_strategy_id/strategies/rows/by_knowledge_base/by_intent/anomalies/latest`，`?strategy_window_days=30` 空样本时也保留近 30 天窗口；生产 390px/1440px 无横向溢出、无控制台错误 |
| RAG 策略效果按部门/岗位拆分 | 本地 `npm run build && npm run typecheck` 通过；本地 Playwright 只读用例通过；远端 `npm run build && npm run typecheck` 通过，PM2 仅重载 `tianrui-ai-support` 且 online；生产 `/api/admin/qa-tests?strategy_window_days=90` 第 4 次重试返回 200，`strategyTrend` 包含 `by_department/by_position`，当前 9 条事件、2 条策略汇总、1 条部门拆分、1 条岗位拆分、6 条异常；生产 `/admin/qa-tests` 1440px/390px 展示“策略效果趋势”“按部门拆分”“按岗位拆分”和异常状态，无页面级横向溢出、无控制台错误 |
| RAG 策略异常一键转整改 | 本地 `npm run build && npm run typecheck` 通过；本地 QA Playwright 回归通过；远端 `npm run build && npm run typecheck` 通过，PM2 仅重载 `tianrui-ai-support` 且 online；生产 `/api/admin/qa-tests?strategy_window_days=90` 返回 9 条趋势事件、6 条异常、4 条可生成整改异常，`anomalies[].suggested_test_ids` 和 `strategies[].candidate_test_ids` 均存在；QA 用量读取超时保护从 2 秒调到 8 秒并增加轻量重试，减少看板误归零；生产 `/admin/qa-tests` 1440px/390px 均展示 4 个“生成相关整改”按钮，无横向溢出、无控制台错误 |
| RAG 策略异常定时巡检 | 本地 `npm run build && npm run typecheck` 通过；本地 QA Playwright 回归通过；本地立即巡检 API 返回 9 条事件、4 个 QA 样本、2 条候选、新增 0、跳过 2；远端 `npm run build && npm run typecheck` 通过，PM2 仅重载 `tianrui-ai-support` 且 online；生产 `/api/admin/qa-tests/strategy-anomaly-schedule` GET/POST 均返回 200，立即巡检返回 9 条事件、4 个 QA 样本、2 条候选、新增 0、跳过 2、`run_count=1`；生产 `/admin/qa-tests` 1440px/390px 展示“策略异常巡检”和“立即巡检”，无横向溢出、无控制台错误 |
| RAG 策略异常运营告警入口 | 本地 `npm run build && npm run typecheck` 通过；本地相关 Playwright 回归 3 条通过；本地 `/admin/insights` 1440px/390px 无横向溢出、无控制台错误，显示“运营告警”和 `?tab=qa` 深链；远端 `npm run build && npm run typecheck` 通过，PM2 仅重载 `tianrui-ai-support` 且 online；生产 `/api/admin/insights` 返回 `totals.operationAlerts=1`，告警为 `qa_strategy_anomaly`，候选 2、新增 0、跳过 2；生产 `/admin/insights` 1440px/390px 无横向溢出、无控制台错误，展示“QA 策略异常需要跟进”和“候选：2”，`/admin/insights?tab=qa` 自动打开 QA 整改；生产 Playwright 相关 3 条回归通过 |
| QA 治理前后效果对比 | 生产 `/api/admin/qa-tests/governance-impact?limit=60` 返回 42 条样本、16 个文档、110 个分片；当前无待确认建议且无正式治理字段，所以前后高覆盖率均为 62%、平均覆盖均为 68.86%；接口 8 秒保护超时时返回 200 降级面板；页面点击“运行对比”后 390px/1440px 均渲染结果且无横向溢出 |
| QA 自动治理动作建议 | 生产 `/api/admin/qa-tests/governance-recommendations?limit=60` 返回 42 条样本、16 个文档、110 个分片、18 条建议、7 条高优先级，最优策略 `content_first`；页面点击“生成建议”后展示建议总数、四类动作计数、优先处理建议，并提供“查看相关题/定位题目/打开资料治理/生成高优先级整改”；`/api/admin/qa-tests/remediation` 支持 `test_ids` 指定生成，生产不存在 ID 验证 200 且 3 秒内返回；页面拦截验证高优先级整改按钮会提交 7 个 QA ID；待治理分片队列汇总 9 个唯一分片、6 个高优先级，支持打开首个分片、展开清单、复制全部治理链接；390px 和 1440px 无横向溢出、无控制台错误、无 4xx/5xx |
| QA 整改复测闭环 | 生产 `/api/admin/qa-tests` 返回 42 条测试、`remediationTasksStatus=ready`、31 条关联整改、存在最近复测；生产 `/admin/qa-tests` 展示“整改任务与自动复测”面板，1440px 显示 31 条整改、31 条复测通过、1 条已复测；390px 手机无横向溢出、无加载失败，整改任务读取慢时显示“整改任务读取超时”和重读入口；本地/远端 typecheck 与 build 通过，PM2 online |
| QA 整改自动复测队列 | 本地和远端 `npm run typecheck && npm run build` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产 `/admin/qa-tests` 展示“复测待整改”按钮，当前待处理/处理中为 0 时按钮禁用；生产空队列 API 验证返回 `completed`、`total=0`、`failed=0`；390px/1440px 无页面级横向溢出、无控制台错误 |
| QA 复测趋势报表 | 生产 `/api/admin/qa-tests` 连续 3 次返回 200、42 条测试、31 条整改任务、`remediationRetestTrend.total_retests=1`；生产 `/admin/qa-tests` 展示“复测趋势”“累计复测 1 次，覆盖 1/31 个整改任务，通过率 100%”；390px/1440px 无横向溢出、无控制台错误；QA 主接口对测试集/知识库必需读库增加 3 次轻量重试，整改任务读库失败会正确降级为 timeout |
| QA 定时自动复测计划 | 本地 `npm run typecheck` 和 `npm run build` 通过；远端 `npm run typecheck && npm run build` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产 Playwright 只读用例验证 `/admin/qa-tests` 展示“自动复测计划”、开启/暂停按钮、立即按计划复测按钮，并验证 `/api/admin/knowledge-tasks/retest-schedule` 返回 `enabled/mode/limit/interval_minutes`；390px/1440px 无横向溢出、无控制台错误 |
| 知识治理后自动排队复测 | 本地 `npm run build && npm run typecheck` 通过；远端 `npm run build && npm run typecheck` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产只读冒烟验证 `/api/documents/chunks/pending-suggestions` 返回 200，`/admin/documents` 在 390px/1440px 无横向溢出、无控制台错误；线上真实资料未做写入式冒烟，避免影响现有知识库内容 |
| 知识库后台 UI | 生产 390px 手机无横向溢出、资料卡片可见、预览面板展开后仍无横向溢出；1440px 桌面表格保留；控制台无报错 |
| 知识库后台加载/空状态/错误重试 | 生产 `/admin/documents` 在 390px/1440px 延迟 5 个知识库相关接口时显示结构化 skeleton；知识库为空和当前知识库无资料均有明确空状态；模拟 `/api/documents` 500 后显示页内“重试”，点击后恢复正常；无 4xx/5xx、无控制台错误、无页面横向溢出 |
| OCR 原图分页预览 | 本地 `npm run build && npm run typecheck` 通过；远端 `npm run build && npm run typecheck` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；生产临时 PNG OCR 资料返回源图 `image/png 900x420`，预览展示“原页图像”和“识别文本与分片治理”；1440px 源图渲染宽度 650px、390px 无横向溢出、无控制台错误；预览接口遇到远程 MySQL/网络抖动时前端会自动重试并保留“重试预览”兜底；临时资料已删除 200 |
| 知识分片质量 | 生产 `/api/documents` 200，真实 Kass PPT 返回 90 分片、平均 83 tokens、质量 96 分、5 个过短分片；390px/1440px 面板无横向溢出 |
| 知识分片治理 | 生产临时 TXT 回归：上传解析 1 分片，摘要/关键词/同义词保存 200，拆分 2 分片并生成 v2 快照，合并回 1 分片并生成 v3 快照，临时资料删除 200；生产 390px/1440px 治理区无横向溢出 |
| 批量分片摘要建议 | 生产 `/api/documents/[id]/chunks/suggestions` 对真实分片返回 200，生成摘要、7 个关键词、4 个同义词；生产知识库页面 390px 点击“生成本页建议”后显示“AI 治理建议”，390px/1440px 无横向溢出、无控制台错误 |
| 全库分片治理队列 | 生产 `/api/documents/chunks/suggestion-jobs` 返回 200；1 分片知识库启动队列后生成 1 条待确认建议，`pending_suggestions` 从 0 变 1；深链预览自动显示“AI 治理建议”和“填入表单”；390px/1440px 无横向溢出、无控制台错误 |
| 分片治理建议确认台 | 生产 `/api/documents/chunks/pending-suggestions` 返回 200；知识库后台显示“待确认治理建议”，支持多选、批量保存、批量撤销、打开定位；生产真实保存 1 条建议后待确认列表变空，分片写入正式 `summary/keywords/synonyms`，`pending_suggestion=false`，`governance_action=pending_suggestion_apply`；390px/1440px 无横向溢出、无控制台错误 |
| 知识治理审计 | 生产 `/api/documents/chunks/governance-audit?limit=60` 返回 200；`/api/documents/chunks/pending-suggestions` 改为只查带待确认建议的分片，`/api/documents/chunks/suggestion-jobs` 统计超时时软降级为 200；知识库后台 390px/1440px 展示“知识治理审计”，无加载失败、无横向溢出、无控制台错误 |
| 资料版本对比/回滚体验 | 本地 `npm run build && npm run typecheck` 通过；本地 `/admin/documents` 点击真实 Kass PPT v1“对比”后展示“版本对比”、未变化/已变更/新增/删除概览、历史版本/当前内容左右栏；390px 无横向溢出、无控制台错误；远端 `npm run build && npm run typecheck` 通过，PM2 仅重载 `tianrui-ai-support` 且 online；生产 `/admin/documents` 点击同一版本对比后 1440px/390px 渲染正常、无控制台错误 |
| 后台低频操作确认 | 本地知识库后台“删除资料”显示页内确认弹窗，取消后未执行删除；本地培训后台桌面/390px 手机“删除课程”显示页内确认弹窗且无横向溢出；生产远端 `npm run typecheck && npm run build` 通过，PM2 online，服务器源码扫描 `components/app` 无 `window.confirm` |
| 配置/用户/洞察后台 UI | 生产 390px/1440px 无横向溢出；用户管理手机卡片编辑、洞察用量手机卡片、配置展开表单均无控制台报错 |
| 员工端/管理员端角色分流 | 本地 `npm run typecheck && npm run build` 通过，相关 Playwright 5 条回归通过；远端 `npm run typecheck && npm run build` 通过，PM2 仅重载 `tianrui-ai-support` 且 online；生产 Playwright 验证管理员工作台、员工首页跳转和手机完整菜单 3 条通过；生产管理员 `/admin/insights` 390px/1440px、员工 `/chat` 390px 均无横向溢出和控制台错误，员工页面管理链接为 0 |
| 配置页加载/错误/空状态 | 配置页拆分健康检查错误和配置文件错误；配置读取失败时只在“页面配置”卡片内重试且不允许空模板误保存，健康检查失败时保留配置表单并单独重试；生产 `/api/system/health` 和 `/api/system/settings` 均返回 200；`/admin/settings` 展示页面配置、配置类别、已就绪/待完善指标；390px/1440px 无页面级横向溢出、无控制台错误 |
| 高频页面加载/错误重试 | 生产 `/training`、`/admin/training`、`/admin/qa-tests` 在 390px/1440px 延迟接口时均显示 skeleton，真实内容加载后无横向溢出、无 4xx/5xx、无控制台错误；模拟首个接口 500 后三页均显示“重试”，点击后恢复正常 |
| 用户/洞察加载/错误重试 | 生产 `/admin/users`、`/admin/insights` 在 390px/1440px 延迟接口时均显示 skeleton，真实内容加载后无横向溢出、无 4xx/5xx、无控制台错误；模拟 `/api/users` 和 `/api/admin/insights` 500 后均显示“重试”，点击后恢复正常 |
| 低频验收页加载/错误重试 | 生产 `/admin/deploy`、`/admin/pilot` 在 390px/1440px 正常态无横向溢出、无控制台错误；延迟 `/api/admin/deploy-readiness` 和 `/api/admin/pilot-readiness` 时显示结构化 skeleton；模拟 500 时显示“生产部署检查加载失败/试运行验收加载失败”和重试按钮；部署检查长 URL 已在手机断行，移动端顶部快捷入口改为两列网格避免撑宽 |
| 低频验收页 Toast 反馈统一 | 本地 `npm run build && npm run typecheck` 通过；远端 `npm run build && npm run typecheck` 通过；PM2 仅重载 `tianrui-ai-support` 且 online；部署检查的模型测试、语音测试、整理演示数据和三类导出改为全局 Toast；试运行验收的导出和生成测试模板改为全局 Toast；生产 `/admin/deploy`、`/admin/pilot` 在 390px/1440px 无横向溢出、无控制台错误，旧页面内局部 notice 不再出现 |
| 安全审计看板 | 生产 `/admin/insights` 安全审计 tab 展示安全事件、待处理、高危待处理、筛选命中和待处理风险构成；支持类型/等级/状态筛选、重置筛选、事件状态下拉处置和跳转会话审计；390px/1440px 无横向溢出、无 4xx/5xx、无控制台错误 |
| 安全事件真实回归 | 生产 `/api/chat` 使用假知识库 ID 触发受控安全测试，接口返回 403 且不进入模型生成；写入 6 条安全事件，包含提示词注入、敏感输入、异常访问和连续风险触发；安全 tab 筛选提示词注入命中 4 张事件卡，UI 可将事件标记为忽略，测试事件最终均为 ignored |
| 企业资料审批与单文档 ACL 闭环 | 新增草稿、审核中、已通过待发布、已驳回、已发布、已归档完整状态；支持提交、撤回、审核通过、必填原因驳回、正式发布、归档和恢复草稿；知识库管理员、部门负责人、安全审核员、质量审核员可按知识库、部门、密级配置审核/发布范围；文档支持部门、岗位、系统角色、账号密级、指定员工 ACL 与可复用权限模板；`/approvals` 提供待我处理、我提交的、已驳回、已发布、批量通过/驳回/发布、完整时间线、确认框、Toast、Skeleton、空状态和错误重试；越权审批写入 `document_approval_acl` 异常访问审计；版本创建增加并发幂等保护；最终批量驳回补丁已部署腾讯云，远程 MySQL 瞬时超时场景已加入严格业务断言下的读取重试；本地 `typecheck/build` 通过，生产远端 `typecheck/build` 通过，PM2 仅重载 `tianrui-ai-support` 且持续 online，生产定向审批回归 2/2、`PLAYWRIGHT_SEED_DEMO=0` 全量 Playwright 15/15 通过；健康检查 19 项红项 0，部署检查 29 项红项 0，试运行得分 100%；累计清理验收测试创建的 85 个账号、17 份资料、17 条审批授权及相关越权测试事件，最终匹配规则下账号、资料、授权均为 0，不影响原业务数据。 |
| 自动化测试/CI 回归底座 | 本地 `npm run typecheck` 通过；本地 `npm run build` 通过；`.github/workflows/ci.yml` 和 `.github/workflows/deploy.yml` YAML 解析通过；生产只读 Playwright 用例“问答测试后台暴露整改复测队列和趋势数据”通过，验证 `/admin/qa-tests` 页面与 `/api/admin/qa-tests` 的 `remediationRetestTrend` 和 `strategyTrend` 数据结构 |
| 生产发布前自动检查 | 已完成并生产验证 | 新增 `deploy:preflight`，检查生产 `AUTH_SECRET/APP_BASE_URL/MYSQL_AUTO_MIGRATE`、MySQL 连接与 26 张核心表、磁盘、最近备份、最近恢复演练、`/login=200` 和 `/api/auth/me=401`；生产实测 8/8 通过。 |
| 分阶段发布与失败保护 | 已完成并生产验证 | 新增 `deploy:package`，在 staging 执行 `npm ci/typecheck/build/preflight` 后才切换；缺少 `package-lock.json` 的无效包被切换前拦截，生产构建哈希、源码哈希、PM2 重启次数均未变化；正式包发布后健康检查 `200/401`。 |
| 快速回滚与数据目录保护 | 已完成并生产验证 | 新增 `deploy:rollback`；生产真实从构建 `ybrxGOoL0YV2ydctEuINc` 回退到 `3TjIM7DFsNXwxRPdMr53x` 后再恢复最终构建，两个方向健康检查均通过；`.env.local/.data/uploads/backups/.ops` 使用 rsync protect 规则，回滚前后备份/恢复状态 SHA256 不变、备份文件仍在；只重载 `tianrui-ai-support`。 |
| MySQL 最新备份恢复演练 | 已完成并生产验证 | 2026-07-11 生成最新压缩备份，恢复到 `_rv_` 隔离前缀表；按备份文件不可变快照核对 26 张表、1474 行一致，临时对象自动清理；预检读取备份与恢复状态均通过。 |
| 企业试运行最终验收 | 已完成并生产验证 | 最终本地 `typecheck/build` 通过，完整 Playwright 17/17、验收页定向 4/4；腾讯云 staging `typecheck/build` 通过，最终生产 Playwright 17/17；`/admin/pilot` 10/10 ready、100 分、红项 0，`/admin/deploy` 31 项、90 分、红项 0；生产 PM2 五个既有服务全部 online。 |
| 二期培训闭环收官 | 已完成并生产验证 | 在原远程 `customerservice` 库内完成表结构升级，表数由 26 增加到 30，没有搬迁或更换数据库；保留 6 门历史课程和 3 条历史测验；备份恢复演练核对 30 表、3083 行；发布前预检 8/8，生产 `/login=200`、`/api/auth/me=401`，培训定向 Playwright 3/3 通过，临时账号和学习进度残留为 0；发布只重载 `tianrui-ai-support`，5 个 PM2 服务全部 online。 |
| 试运行指标稳定读取 | 已完成并生产验证 | MySQL 模式改为单连接批量聚合知识库、资料、分片、账号、QA、培训和反馈指标，并持久化 5 分钟真实快照；清理测试数据后生产返回 201 分片、42 条 QA、39 条已运行、90% 通过率、6 个培训任务，避免远程 MySQL 抖动误报 0 和红项。 |
| 验收测试数据清理 | 已完成 | 精确清理所有 `*.acl-*@tianrui.local` 测试账号、`审批验收资料 ACL-*`、`闭环工单 TICKET-E2E-*` 及关联审批、版本、通知、安全事件；最终匹配账号、资料、工单、安全事件、通知均为 0。 |

## 下一步建议顺序

1. 运营积压处理：关闭超时工单，处理 7 条高频知识整改，并持续观察 QA 通过率。
2. 数字人/统一身份：等域名 HTTPS 或第三方数字人参数就绪后，继续做正式联调。
3. 外部通知：按企业条件接入企业微信或邮件 Webhook，站内通知作为可追踪主记录。
4. GitHub CI/CD 首次云端运行：配置部署 Secrets，推送仓库后跑一次 CI，再手动触发 Deploy 验证生产发布链路。
